fix: Update CDN image path for latest article and PM2 process name

Image Path Corrections:
- Fixed hero image URL in app/blog/page.tsx (kardtrust → budgetbee)
- Fixed hero image URL in app/personal-finance/page.tsx (kardtrust → budgetbee)
- Ensures consistent CDN subdomain usage across all new content

Deployment Script Fix:
- Updated PM2 process name in scripts/deploy_update.sh
- Changed "budgetbee" to "budgetbee-next" to match actual process name
- Prevents deployment restart failures

Technical Details:
- All new articles now use correct budgetbee subdomain for images
- Maintains consistency with updated generation instruction files
- Deploy script now correctly restarts the active PM2 process
