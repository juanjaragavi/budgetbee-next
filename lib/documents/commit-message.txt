fix(types): resolve all 21 TypeScript errors across Button, carousel, and financial-solutions pages

- add "outline" variant and optional "size" prop to custom Button component
- export buttonVariants() helper for use in alert-dialog and other Shadcn-style consumers
- rewrite CarouselPrevious/CarouselNext to use native <button> elements; resolves disabled-prop union-type errors and removes unused Button import from carousel.tsx
- all 16 financial-solutions pages using variant="outline" now type-check correctly
- tsc --noEmit exits 0; next lint reports no warnings or errors
