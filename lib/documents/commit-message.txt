fix(ads): prevent race condition wiping TopAds iframes on SPA transitions

- Remove delayed topAds.spa() trigger after reinjectTopAdsScript
- The re-injected TopAds script auto-initializes upon load, so triggering .spa() was destructively wiping the just-loaded ad creatives and leaving only the 'Advertising' placeholder text.
