feat: polish dev toolbar and mobile navigation styling

- add a dismiss control to the AdZep development test panel so it can be hidden without disabling dev mode
- reuse a shared class for mobile header links and tweak logo sizing for better visual balance
- enlarge and restyle the back-to-top fab to match the updated yellow action button treatment
