fix(ads): fix ad units not rendering on gaming Promise/Reward pages after SPA navigation

- Replace fixed 100ms timeout in TopAdsSPAHandler with DOM polling mechanism
- Poll for [data-topads] containers every 200ms (up to ~3s) before triggering topAds.spa()
- Fixes ads not appearing when quiz redirects to Promise/Reward pages via router.push()
- Root cause: ad containers weren't in DOM yet when topAds.spa() fired after only 100ms