fix: scope /api/ disallow to wildcard user-agent in robots.txt

Add explicit User-agent: * directive before the Disallow: /api/
rule so the disallow applies only to general crawlers, not to the
Mediapartners-Google agents that were just granted access above.