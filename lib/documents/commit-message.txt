fix: handle absolute URLs for gaming quiz redirects

- Updated `GamingQuizEntry` handleCta function to use `window.location.href` for external destinations (URLs starting with 'http').
- Kept `router.push` as fallback for Next.js internal client-side routes.
