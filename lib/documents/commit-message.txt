fix(ads): comprehensive TopAds SPA handler for gaming entry→promise transitions

- Track previous pathname to detect quiz/chat→promise/reward transitions
- Add isGamingEntryPage() to identify both excluded quiz pages and chat pages
- Re-apply topAds.config to clear cached exclusion state on entry transitions
- Re-inject topAds.min.js script on entry→promise navigation for fresh init
- Add follow-up topAds.spa() trigger 2s after re-injection
- Extend polling window to 5s (25×200ms) for entry page transitions
- Skip initial activation on excluded pages to prevent unnecessary processing