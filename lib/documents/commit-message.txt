fix(analytics): disable TopAds refresh behavior

- add explicit TopAds refresh settings with inactive status
- prevent standard and anchor refresh cycles from auto-running
- keep existing lazy loading and path exclusions unchanged
