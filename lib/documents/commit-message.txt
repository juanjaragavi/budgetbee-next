fix(gaming): remove duplicate Advertising labels and unnamed ad slots

- Remove all <p>Advertising</p> wrapper labels from gaming ad slots across
  quiz-entry, chat-entry, promise-template, reward-template, and FAQ accordion
- Remove unnamed inline data-topads divs (no square0{n} id) from quiz and
  chat components to eliminate stacked disclosure labels
- Remove unnamed MOB ad pairs from promise and reward templates
- Relocate square01 slot from hero section to after first content section
  in promise and reward templates for better ad placement
- Deduplicate Google Ads gtag config call (single config with all options)
- Improve sticky bottom banner responsive layout for mobile viewports
- Delete unused GoogleTagManager.tsx component (replaced by analytics module)
