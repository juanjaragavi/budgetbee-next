chore: clean up redundant wildcard /api/ allow rule in robots.txt

Remove duplicate User-agent: * Allow: /api/ block since the global
Allow: / already covers /api/ for all crawlers. Keep only the
Mediapartners-Google specific rules.